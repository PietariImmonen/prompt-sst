appId: io.sstreplicache.clyo
productName: Clyo Desktop
artifactName: clyo-desktop-${version}-${os}-${arch}.${ext}
directories:
  buildResources: build
  output: dist
files:
  - '!**/.vscode/*'
  - '!src/*'
  - '!electron.vite.config.{js,ts,mjs,cjs}'
  - '!{.eslintignore,.eslintrc.cjs,.prettierignore,.prettierrc.yaml,dev-app-update.yml,CHANGELOG.md,README.md}'
  - '!{.env,.env.*,.npmrc,pnpm-lock.yaml}'
  - '!{tsconfig.json,tsconfig.node.json,tsconfig.web.json}'
asarUnpack:
  - resources/**
icon: build/icon.png
win:
  executableName: clyo-desktop
nsis:
  artifactName: clyo-desktop-${version}-windows.${ext}
  shortcutName: ${productName}
  uninstallDisplayName: ${productName}
  createDesktopShortcut: always
mac:
  category: public.app-category.productivity
  hardenedRuntime: true
  gatekeeperAssess: false
  entitlements: build/entitlements.mac.plist
  entitlementsInherit: build/entitlements.mac.plist
  extendInfo:
    - NSCameraUsageDescription: Application requests access to the device's camera.
    - NSMicrophoneUsageDescription: Application requests access to the device's microphone.
    - NSDocumentsFolderUsageDescription: Application requests access to the user's Documents folder.
    - NSDownloadsFolderUsageDescription: Application requests access to the user's Downloads folder.
  notarize:
    appBundleId: io.sstreplicache.clyo
    teamId: ${APPLE_TEAM_ID}
dmg:
  artifactName: clyo-desktop-${version}-mac.${ext}
linux:
  target:
    - AppImage
    - snap
    - deb
  maintainer: electronjs.org
  category: Utility
appImage:
  artifactName: clyo-desktop-${version}-linux.${ext}
npmRebuild: false
publish:
  provider: github
  owner: PietariImmonen
  repo: prompt-sst
  releaseType: release
electronDownload:
  mirror: https://npmmirror.com/mirrors/electron/
